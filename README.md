# TypeScript Deep Dive: Interface vs Type, any, unknown, never

একটি পরিষ্কার, স্থিতিশীল এবং maintainable TypeScript কোডবেস তৈরি করতে হলে TypeScript-এর type system সম্পর্কে গভীর ধারণা থাকা জরুরি। অনেক নতুন ডেভেলপার—আমিও তাদের একজন—শুরুতে মনে করতাম `any` ব্যবহার করলেই সব সমস্যা দূর হয়ে যায়। কিন্তু প্রোডাকশন-লেভেলের অ্যাপ্লিকেশনে কাজ করা শুরু করলেই বোঝা যায়: **`any` আসলে নীরব শত্রু।**

এটি হঠাৎ করে error, unpredictable behavior এবং এমনসব bug তৈরি করে যেগুলো খুঁজে বের করা অত্যন্ত কঠিন হয়ে যায়।

এই README-তে বাস্তব অভিজ্ঞতা থেকে TypeScript-এর পাঁচটি গুরুত্বপূর্ণ ধারণা ব্যাখ্যা করা হয়েছে:
**Interface, Type Alias, any, unknown, এবং never।**

---

## 🚀 পার্ট ১: Interface এবং Type — বেসিক থেকে অ্যাডভান্সড বোঝাপড়া

TypeScript-এর সবচেয়ে বেশি ভুল বোঝা ধারণাগুলোর একটি হলো Interface এবং Type-এর পার্থক্য। দুটোই object structure define করে, কিন্তু তাদের উদ্দেশ্য, ক্ষমতা এবং ব্যবহার আলাদা।

---

## 🧩 Interface — পরিষ্কার, সম্প্রসারণযোগ্য, এবং Domain Modeling-এর জন্য আদর্শ

Interface মূলত structure-first design-এর জন্য উপযোগী। এটি extend করা সহজ এবং বড় কোডবেসে readability বজায় রাখে।

```ts
interface User {
  id: number;
  name: string;
  email: string;
}
```

Extend করা খুব সহজ:

```ts
interface Admin extends User {
  role: "super-admin" | "editor";
}
```

**Use cases:**

- Domain Model
- DTO (Data Transfer Object)
- API Schema
- Class implementation

---

## 🧰 Type Alias — flexibility এবং complex type composition-এর রাজা

Interface-এর বিপরীতে, Type Alias শুধু object নয় — আরও অনেক কিছু define করতে পারে।

```ts
type Status = "active" | "inactive" | "pending";
```

Generic, reusable type তৈরি করতেও Type Alias অসাধারণ:

```ts
type ApiResponse<T> = {
  data: T;
  success: boolean;
  error?: string;
};
```

**সংক্ষেপে:**

- **Interface = structural blueprint**
- **Type Alias = complex type তৈরির শক্তিশালী টুল**

---

## ⚠️ `any`: শুরুতে ভালো লাগে, কিন্তু শেষে বিপদ ডেকে আনে

`any` TypeScript-কে বলে:

> "আমাকে কিছু জিজ্ঞেস করো না, আমি সব করতে পারব।"

এজন্যই `any` খুব দ্রুত মনে হয় সুবিধাজনক, কিন্তু এটি পুরো টাইপ সেফটি নষ্ট করে দেয়।

```ts
async function sendEmail(emailData: any) {
  const recipient = emailData.to.toLowerCase();
}
```

যদি API থেকে `to` array আসে?

- ❌ অ্যাপ্লিকেশন ক্র্যাশ করবে
- ❌ Debug করা কঠিন
- ❌ কোনো warning বা type error নেই

বড় প্রজেক্টে `any` যতটা স্বাধীনতা দেয়, তার চেয়ে বেশি ক্ষতি করে।

---

## 🛡️ `unknown`: নিরাপদ any

`unknown` ডেভেলপারকে বাধ্য করে ডেটা validate করতে:

```ts
const data: unknown = await response.json();

if (!isValidUser(data)) {
  throw new Error("Invalid user data");
}

return data;
```

**Benefits:**

- ✓ Runtime error কমে
- ✓ ডেটা validate করা বাধ্যতামূলক
- ✓ External API এবং form input নিরাপদ হয়

---

# 🔥 পার্ট ২: never, Interface vs Type — অ্যাডভান্সড ধারণা

---

## 🧩 `never`: Exhaustive Checking এর শক্তিশালী অস্ত্র

`never` হল সেই টাইপ যা কখনো occur করে না। এটি মূলত compiler-কে বলে দেয় যে "এই অবস্থাটা অসম্ভব হওয়া উচিত"।

```ts
function getEmailStatusLabel(status: "draft" | "sent" | "failed") {
  switch (status) {
    case "draft":
      return "📝 Draft";
    case "sent":
      return "✅ Sent";
    case "failed":
      return "❌ Failed";
    default:
      const exhaustive: never = status; // 🚨 ensures all cases handled
      return exhaustive;
  }
}
```

নতুন status যোগ করলে TypeScript error দেবে — ফলে কোনো case বাদ পড়ে থাকার সম্ভাবনা থাকে না।

---

## 🏗️ Interface vs Type — পেশাদার তুলনা

| Feature              | Interface    | Type Alias       |
| -------------------- | ------------ | ---------------- |
| Object Structure     | ✅ Excellent | ✅ Excellent     |
| Union Types          | ❌           | ✅ Strong        |
| Intersection         | ⚠️ Limited   | ✅ Flexible      |
| Declaration Merging  | ✅ Supported | ❌ Not Supported |
| Class Implementation | ✅ Natural   | ⚠️ Less Common   |
| Extending            | Excellent    | Strong (via `&`) |

---

## 🧭 Practical Tips (Real-world Experience)

### ✔ Interface ব্যবহার করুন যখন:

- API response মডেল করতে চান
- Class implement করতে হবে
- Shared configuration তৈরি করবেন
- Domain-driven design করছেন

### ✔ Type Alias ব্যবহার করুন যখন:

- Union types দরকার
- Multiple type combine করতে চান
- Conditional বা advanced type দরকার
- Generic reusable টাইপ লিখছেন

### ✔ `unknown` ব্যবহার করুন যখন:

- API data unpredictable
- User input validate দরকার
- Type-safe parsing করতে চান

### ✔ `never` ব্যবহার করুন যখন:

- Exhaustive switch-case চান
- Impossible state detect করতে চান

---

## ✅ উপসংহার: TypeScript মানে নিরাপদ এবং আত্মবিশ্বাসী সফটওয়্যার ডেভেলপমেন্ট

TypeScript শুধু error কমানোর টুল নয়—এটি শেখায় কিভাবে নিরাপদ, স্পষ্ট, maintainable কোড লেখা যায়।

- ✔ `any` স্বাধীনতা দেয়, কিন্তু বিপজ্জনক
- ✔ `unknown` নিরাপত্তা বাড়ায়
- ✔ `never` discipline নিশ্চিত করে
- ✔ Interface + Type — দুটোই দরকার, কিন্তু কাজ অনুযায়ী সঠিকটি বাছাই করাই দক্ষতার পরিচয়

**TypeScript শেখা মানে শুধু syntax শেখা নয়—software design চিন্তাধারা উন্নত করা।**

💙 শুভ TypeScript কোডিং!
**Smart, safe, and predictable applications তৈরি করতে থাকুন।**
